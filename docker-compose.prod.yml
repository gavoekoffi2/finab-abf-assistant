services:
  finab-abf-assistant:
    build:
      context: /opt/finab-abf-assistant
      dockerfile: Dockerfile.prod
    container_name: finab-abf-assistant
    restart: unless-stopped
    environment:
      - FINAB_PUBLIC_URL=https://abf.finablasolution.cloud
    networks:
      - web
    volumes:
      - finab_abf_data:/app/data
      - finab_abf_output:/app/output
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.finab-abf-temp.rule=Host(`finab-abf.76.13.129.252.sslip.io`)"
      - "traefik.http.routers.finab-abf-temp.entrypoints=websecure"
      - "traefik.http.routers.finab-abf-temp.tls.certresolver=letsencrypt"
      - "traefik.http.routers.finab-abf-final.rule=Host(`abf.finablasolution.cloud`)"
      - "traefik.http.routers.finab-abf-final.entrypoints=websecure"
      - "traefik.http.routers.finab-abf-final.tls.certresolver=letsencrypt"
      - "traefik.http.services.finab-abf.loadbalancer.server.port=8080"

networks:
  web:
    external: true

volumes:
  finab_abf_data:
  finab_abf_output:
